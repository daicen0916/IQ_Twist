package comp1110.ass2;

// Cen Dai
public enum Flipping {
    //according to the rules for building a placement string, the orientation should be an int, also it should be determined by
    //rotation and flipping(both of them are int). In this case, we need to build a enum file so that we can use it to determine the orientation.

    Up,
    Down
}
